// 변수 초기화
let board;

let snake;
let apple;

class Square {
  constructor(x, y) {
    this.x    = x;
    this.y    = y;
    this.back = null;
  }
  draw() {
    
  }
}
class Snake {
    // Initializing an object
    constructor() {
    }
}